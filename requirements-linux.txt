# ============================================================
# Liao - Linux requirements
# ============================================================
#
# System packages (apt) - REQUIRED:
#   sudo apt install xdotool wl-clipboard xclip \
#       tesseract-ocr tesseract-ocr-chi-sim \
#       gnome-screenshot
#
# NOTE: xdotool works for most chat apps on Wayland since they
#       typically run under XWayland compatibility layer.
#
# System packages (apt) - for Wayland screenshot (alternative to gnome-screenshot):
#   sudo apt install gstreamer1.0-plugins-good pipewire \
#       libgirepository1.0-dev gir1.2-gst-plugins-base-1.0 \
#       python3-gi python3-dbus
#
# Then install Python packages:
#   pip install -r requirements-linux.txt
#
# NOTE: gnome-screenshot is the simplest way to capture screenshots on GNOME/Wayland.
#       PyGObject-based capture requires system dev libs and is optional.
# ============================================================

# --- core ---
PySide6>=6.4.0
requests>=2.28.0
Pillow>=9.3.0
numpy>=1.24.0
pyperclip>=1.8.2
pyautogui>=0.9.53

# --- OCR (rapidocr for Python <3.13, pytesseract as universal fallback) ---
rapidocr-onnxruntime>=1.2.0; python_version < "3.13"
pytesseract>=0.3.10

# --- Wayland screenshot support (optional, install via apt if pip fails) ---
# dbus-python>=1.3.2
# PyGObject>=3.42.0
